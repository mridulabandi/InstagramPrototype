# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'InstagramDemo' do
pod 'Alamofire'
pod 'ObjectMapper'
pod "PromiseKit"
pod 'AlamofireImage'
pod 'SwiftyJSON'
platform :ios, '8.0'
pod 'SDWebImage', '~> 5.0'
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for InstagramDemo

  target 'InstagramDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InstagramDemoUITests' do
    # Pods for testing
  end

end
